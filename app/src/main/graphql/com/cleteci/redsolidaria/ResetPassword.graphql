mutation resetPassword($email: String!) {
    passwordReset(email: $email)
}

mutation changePassword($password: String!, $token: String!) {
    changeUserPassword(newPassword: $password, token: $token)
}