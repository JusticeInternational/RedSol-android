query getOrganizationsByCategory($id: ID) {
    ServiceCategory(id: $id) {
        name
        organizations(orderBy: ranking_desc) {
            id
            name
            hourHand
            ranking
            location {
                name
            }
        }
    }
}

